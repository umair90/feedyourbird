config =
{
        debug =
        {
        general = false, -- Turn on general debugging
        assertDialogs = false, -- Display dialog boxes when asserting
        typeChecking = false, -- Turn on type checking by default
        traceGC = false, -- Trace info on object garbage collection

                makePrecompiledLua = false, -- turn on writing of precompiled Lua files
                usePrecompiledLua = false, -- turn on reading of precompiled Lua files
                useConcatenatedLua = false, -- turn on use of concatenated precompiled Lua files, when writing or reading
        }
}